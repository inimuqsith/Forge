[package]
name = "kbd"
version = "2.10.0"
release = 1
slot = "0"
description = "Keytable files and keyboard utilities (loadkeys, setfont)"
license = "GPL-2.0-or-later"
upstream = "https://kbd-project.org/"

[dependencies]
runtime = [
    "glibc",
]
build = [
    "make",
    "gcc",
    "bison",
    "flex",
    "pkgconf",
]

[sources]
urls = [
    "https://kernel.org/pub/linux/utils/kbd/kbd-2.10.0.tar.xz",
]
sha256 = ["6e5ca4f8d76ee9e3a8db700b667f13e12aac9933828a64e1aaad93d26be9b479"]

[build]
type = "autotools"
script = """
cd "${srcdir}/kbd-${pkgver}"
./configure --prefix=/usr --disable-vlock
make ${MAKEFLAGS}
make DESTDIR="${DESTDIR}" install
"""
