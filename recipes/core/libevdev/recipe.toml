[package]
name = "libevdev"
version = "1.13.3"
release = 1
slot = "0"
description = "Wrapper library for evdev devices"
license = "MIT"
upstream = "https://www.freedesktop.org/wiki/Software/libevdev/"

[dependencies]
runtime = ["glibc"]
build = ["meson", "ninja", "gcc", "pkgconf", "python"]

[sources]
urls = [
    "https://www.freedesktop.org/software/libevdev/libevdev-1.13.3.tar.xz"
]
sha256 = [
    "abf1aace86208eebdd5d3550ffded4c8d73bb405b796d51c389c9d0604cbcfbf"
]

[build]
type = "meson"
script = """
cd "${srcdir}/libevdev-${pkgver}"
meson setup build --prefix=/usr --buildtype=release -Ddocumentation=disabled -Dtests=disabled
ninja -C build -j${FORGE_JOBS:-$(nproc)}
DESTDIR="${DESTDIR}" ninja -C build install
"""
