[package]
name = "vulkan-loader"
version = "1.4.364"
release = 1
slot = "0"
description = "Vulkan Installable Client Driver (ICD) Loader"
license = "Apache-2.0"
upstream = "https://github.com/KhronosGroup/Vulkan-Loader"

[dependencies]
runtime = ["glibc", "vulkan-headers", "wayland"]
build = ["cmake", "ninja", "pkgconf", "python"]

[sources]
urls = [
  "https://github.com/KhronosGroup/Vulkan-Loader/archive/refs/tags/v1.4.363.tar.gz"
]
sha256 = [
    "2d5e5a84e83c5360a828d02d828e900380ecf1ac8c7d3182650b7755d11cf536"
]

[build]
type = "cmake"
script = """
cmake -B build -G Ninja \
  -DCMAKE_INSTALL_PREFIX=/usr \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_INSTALL_SYSCONFDIR=/etc \
  -DCMAKE_INSTALL_LIBDIR=lib \
  -DBUILD_TESTS=OFF

ninja -C build
DESTDIR="${DESTDIR}" ninja -C build install
"""
