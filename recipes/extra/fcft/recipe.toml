[package]
name = "fcft"
version = "3.3.3"
release = 1
slot = "0"
description = "Simple library for font loading and glyph rasterization"
license = "MIT"
upstream = "https://codeberg.org/dnkl/fcft"

[dependencies]
runtime = ["glibc", "fontconfig", "freetype", "pixman", "libutf8proc"]
build = ["meson", "ninja", "gcc", "pkgconf", "tllist"]

[sources]
urls = [
    "https://codeberg.org/dnkl/fcft/releases/download/3.3.3/fcft-3.3.3.tar.gz"
]
sha256 = [
    "bb298772d625e7d917373d541456f34f1957a9e22b16f17f64158b2c3816563c"
]

[build]
type = "meson"
script = """
cd "${srcdir}/fcft"
meson setup build --prefix=/usr --buildtype=release -Ddocs=disabled
ninja -C build -j${FORGE_JOBS:-$(nproc)}
DESTDIR="${DESTDIR}" ninja -C build install
"""
