[package]
name = "clang"
version = "23.1.2"
release = 1
slot = "22"
description = "C, C++, and Objective-C front-end for LLVM (Meta-package provided by monolithic llvm)"
license = "Apache-2.0-with-LLVM-exception"
upstream = "https://clang.llvm.org/"

[dependencies]
runtime = ["llvm"]
build = []

[sources]
urls = []
sha256 = []

[build]
type = "meta"
script = """
echo "Package clang is provided by the monolithic llvm toolchain package."
"""
